  # free up keybindings for copy/paste in zellij after other keybindings are set
    bindkey -r "^V"
    bindkey -r "^v"
    bindkey -r "^[v"
    bindkey -r "^[V"
    bindkey -r "^[c"
    bindkey -r "^[C"
