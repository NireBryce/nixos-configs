{ 
    ...
}:
{
    # TODO: turn into its own module.  helps microphone issues?, reduces latency? does it matter for voice chat? game audio?
    # musnix = {
    #   enable          = true;
    #   kernel.realtime = false;              # you shouldn't enable this unless debugging something
    #   ## ensure realtime processes don't hang the machine
    #     # das_watchdog.enable = true;
    # };
}
